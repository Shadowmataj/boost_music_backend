<header>
    <h1>Lista de productos</h1>
</header>
<div>
    <ul class="products-unlist list-group list-group-numbered">
        {{!-- it gets the products list from the endpoint and deploy them --}}
        {{#each products}}
        <li class="products-list list-group-item">{{title}}, ${{price}}, {{stock}}, {{category}}, {{code}}</li>
        {{/each}}
    </ul>
</div>